const { streamText } = require('ai'); const { openai } = require('@ai-sdk/openai'); async function run() { try { const result = await streamText({ model: openai('gpt-4o-mini'), messages: [{ id: '1', role: 'user', content: 'Hi' }, { id: '2', role: 'assistant', content: 'Hello' }, { id: '3', role: 'user', content: 'test' }] }); for await (const chunk of result.textStream) {} console.log('OK'); } catch(e) { console.log('ERR', JSON.stringify(e.cause, null, 2)) }} run();
